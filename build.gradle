plugins {
  id "org.sonarqube" version "3.4.0.2513"
}

sonarqube {
  properties {
    property "sonar.projectKey", "rinkuAlight_java_sandbox"
    property "sonar.organization", "rinkualight"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}